{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Update",
            "type": "shell",
            "command": "git checkout master  -- books/ collections/ libraries/",
            "group": "build"
        },
        {
            "label": "Generate",
            "type": "shell",
            "command": "./generate/generate.sh ",
            "group": "build"
        }
    ]
}
